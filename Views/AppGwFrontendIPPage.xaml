<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.AppGwFrontendIPPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AzureDesktop.Controls"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Frontend IP Configurations" Style="{StaticResource SubtitleTextBlockStyle}" />
        <controls:DataTableControl x:Name="FrontendIPTable" Grid.Row="1" MaxHeight="300" />

        <TextBlock Grid.Row="2" Text="Frontend Ports" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,16,0,0" />
        <controls:DataTableControl x:Name="FrontendPortsTable" Grid.Row="3" />
    </Grid>
</Page>
