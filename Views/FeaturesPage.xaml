<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:AzureDesktop.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <BreadcrumbBar x:Name="Breadcrumb" Grid.Row="0" ItemClicked="Breadcrumb_ItemClicked" />

        <TextBlock Grid.Row="1" Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Text="Preview Features" />

        <!-- Unified search/filter bar -->
        <StackPanel Grid.Row="2" Margin="0,12,0,0" Orientation="Horizontal" Spacing="8">
            <TextBox Width="220" PlaceholderText="Provider namespace" Text="{x:Bind ViewModel.ProviderFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Width="220" PlaceholderText="Feature name" Text="{x:Bind ViewModel.FeatureFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Command="{x:Bind ViewModel.SearchCommand}" Content="Search" Style="{ThemeResource AccentButtonStyle}" />
            <ProgressRing Width="20" Height="20" IsActive="{x:Bind ViewModel.IsSearching, Mode=OneWay}" Visibility="{x:Bind ViewModel.IsSearching, Mode=OneWay}" />
            <ProgressRing Width="20" Height="20" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
        </StackPanel>

        <!-- Search error -->
        <InfoBar Grid.Row="3" Margin="0,4,0,0" IsClosable="False" IsOpen="{x:Bind ViewModel.SearchError, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}" Message="{x:Bind ViewModel.SearchError, Mode=OneWay}" Severity="Warning" />

        <!-- Search result tile -->
        <Border Grid.Row="4" Margin="0,4,0,0" Padding="16,12" Visibility="{x:Bind ViewModel.SearchResult, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource SystemAccentColorLight2}" BorderThickness="2" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="2">
                    <TextBlock FontWeight="SemiBold" Text="{x:Bind ViewModel.SearchResult.FeatureName, Mode=OneWay}" />
                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind ViewModel.SearchResult.ProviderNamespace, Mode=OneWay}" />
                </StackPanel>
                <Border Grid.Column="1" Padding="8,4" VerticalAlignment="Center" Margin="8,0" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4">
                    <TextBlock FontSize="11" Text="{x:Bind ViewModel.SearchResult.State, Mode=OneWay}" />
                </Border>
                <Button Grid.Column="2" Click="SearchResult_Click" VerticalAlignment="Center" Content="View" />
            </Grid>
        </Border>

        <!-- Two-panel: RP list + features -->
        <Grid Grid.Row="5" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Resource Providers -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,6" Text="Resource Providers" />
                <ListView Grid.Row="1" IsItemClickEnabled="True" ItemClick="Provider_ItemClick" ItemsSource="{x:Bind ViewModel.FilteredProviders, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock FontSize="13" Text="{x:Bind}" TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Features for selected provider -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8">
                    <TextBlock FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Text="{x:Bind ViewModel.SelectedProvider, Mode=OneWay, TargetNullValue='Select a provider'}" />
                    <ProgressRing Width="16" Height="16" IsActive="{x:Bind ViewModel.IsLoadingFeatures, Mode=OneWay}" Visibility="{x:Bind ViewModel.IsLoadingFeatures, Mode=OneWay}" />
                </StackPanel>
                <InfoBar Grid.Row="1" Margin="0,4,0,0" IsClosable="False" IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" Severity="Warning" />
                <ListView Grid.Row="2" Margin="0,4,0,0" IsItemClickEnabled="True" ItemClick="FeatureList_ItemClick" ItemsSource="{x:Bind ViewModel.FilteredFeatures, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,2" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:FeatureEntry">
                            <Border Padding="12,10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock FontWeight="SemiBold" VerticalAlignment="Center" Text="{x:Bind FeatureName}" TextTrimming="CharacterEllipsis" />
                                    <Border Grid.Column="1" Padding="8,4" VerticalAlignment="Center" Margin="8,0" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="4">
                                        <TextBlock FontSize="11" Text="{x:Bind State, Mode=OneWay}" />
                                    </Border>
                                    <FontIcon Grid.Column="2" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Glyph="&#xE76C;" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>
