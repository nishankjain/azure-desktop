<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.AppGwRoutingRuleDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid Padding="16,8,40,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="0" VerticalAlignment="Center">
                <Button x:Name="EllipsisButton" Visibility="Collapsed" Style="{StaticResource BreadcrumbEllipsisStyle}" Content="..." />
                <BreadcrumbBar x:Name="Breadcrumb" ItemClicked="Breadcrumb_ItemClicked">
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{x:Bind}" FontSize="28" FontWeight="SemiBold" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
            </StackPanel>

        <TextBlock x:Name="TitleText" Grid.Row="1" FontSize="20" FontWeight="SemiBold" Margin="0,8,0,8" />

        <Grid Grid.Row="2">
        </Grid>

        <Button Grid.Row="3" Margin="0,8,0,0" Click="Save_Click" Content="Save Changes" Style="{ThemeResource AccentButtonStyle}" />

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="350" MaxWidth="500" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Rule settings (scrollable) -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="FormArea" Spacing="16" />
            </ScrollViewer>

            <!-- Right: Path rules (only visible for path-based routing) -->
            <Grid x:Name="PathRulesPanel" Grid.Column="2" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TextBlock Text="URL Path Map Rules" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Button x:Name="AddPathRuleBtn" Content="Add Path Rule" HorizontalAlignment="Right" Click="AddPathRule_Click" Style="{ThemeResource AccentButtonStyle}" />
                </Grid>
                <TextBlock x:Name="PathMapDefaultText" Grid.Row="1" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,4" Visibility="Collapsed" />
                <ListView x:Name="PathRulesList" Grid.Row="2" SelectionMode="None" Margin="0,4,0,0" />
            </Grid>
        </Grid>
    </Grid>
</Page>
