<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.AppGwSectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid Padding="16,8,40,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8" Orientation="Horizontal" Spacing="0" VerticalAlignment="Center">
                <Button x:Name="EllipsisButton" Visibility="Collapsed" Background="Transparent" BorderThickness="0" Padding="4,0,12,0" VerticalAlignment="Center" FontSize="28" FontWeight="SemiBold" Content="..." />
                <BreadcrumbBar x:Name="Breadcrumb" ItemClicked="Breadcrumb_ItemClicked">
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <TextBlock Text="{x:Bind}" FontSize="28" FontWeight="SemiBold" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
            </StackPanel>

        <Grid Grid.Row="1">
            <StackPanel x:Name="StatusPanel" Orientation="Horizontal" Spacing="8" Padding="12,6" HorizontalAlignment="Right" VerticalAlignment="Center" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Visibility="Collapsed">
                <ProgressRing x:Name="StatusSpinner" Width="14" Height="14" IsActive="True" />
                <FontIcon x:Name="StatusIcon" FontSize="14" Visibility="Collapsed" />
                <TextBlock x:Name="StatusText" FontSize="12" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <StackPanel x:Name="SearchAddPanel" Grid.Row="2" Margin="0,8,0,0" Orientation="Horizontal" Spacing="12" Visibility="Collapsed">
            <TextBox x:Name="SearchBox" Width="300" PlaceholderText="Search by name..." TextChanged="SearchBox_TextChanged" />
            <Button x:Name="AddNewButton" Click="AddNew_Click" Content="Add New" Style="{ThemeResource AccentButtonStyle}" />
        </StackPanel>

        <!-- Row 3 fills remaining space - ContentArea renders the table here -->
        <Grid x:Name="ContentArea" Grid.Row="3" Margin="0,8,0,0" />
    </Grid>
</Page>
