<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.ResourcesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:AzureDesktop.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="16,8,40,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Breadcrumb -->
        <BreadcrumbBar x:Name="Breadcrumb" Grid.Row="0" ItemClicked="Breadcrumb_ItemClicked" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,0"
            FontSize="28"
            FontWeight="SemiBold"
            Text="Resources" />

        <!-- Search + Filters -->
        <StackPanel
            Grid.Row="2"
            Margin="0,12,0,0"
            Orientation="Horizontal"
            Spacing="12">
            <TextBox
                Width="250"
                PlaceholderText="Search by name..."
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Type filter -->
            <DropDownButton Content="Filter by type">
                <DropDownButton.Flyout>
                    <Flyout Placement="BottomEdgeAlignedLeft">
                        <ScrollViewer MaxHeight="300">
                            <ItemsRepeater x:Name="TypeFilterList" ItemsSource="{x:Bind ViewModel.TypeFilters, Mode=OneWay}">
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <CheckBox Content="{x:Bind}" Checked="TypeFilter_Changed" Unchecked="TypeFilter_Changed" />
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </ScrollViewer>
                    </Flyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <!-- Location filter -->
            <DropDownButton Content="Filter by location">
                <DropDownButton.Flyout>
                    <Flyout>
                        <ScrollViewer MaxHeight="300">
                            <ItemsRepeater x:Name="LocationFilterList" ItemsSource="{x:Bind ViewModel.LocationFilters, Mode=OneWay}">
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <CheckBox Content="{x:Bind}" Checked="LocationFilter_Changed" Unchecked="LocationFilter_Changed" />
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </ScrollViewer>
                    </Flyout>
                </DropDownButton.Flyout>
            </DropDownButton>

            <Button Click="ClearFilters_Click" Content="Clear Filters" />
        </StackPanel>

        <!-- Sort buttons with indicator -->
        <StackPanel
            Grid.Row="3"
            Margin="0,8,0,8"
            Orientation="Horizontal"
            Spacing="8">
            <TextBlock
                FontSize="12"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                VerticalAlignment="Center"
                Text="Sort by:" />
            <Button x:Name="SortNameButton" Click="SortByName_Click" FontSize="12" Padding="8,4" Content="Name ↑" />
            <Button x:Name="SortTypeButton" Click="SortByType_Click" FontSize="12" Padding="8,4" Content="Type" />
            <Button x:Name="SortLocationButton" Click="SortByLocation_Click" FontSize="12" Padding="8,4" Content="Location" />
            <ProgressRing
                Width="20"
                Height="20"
                IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
        </StackPanel>

        <InfoBar
            Grid.Row="4"
            IsClosable="False"
            IsOpen="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
            Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
            Severity="Warning" />

        <!-- Grouped resource list -->
        <ScrollViewer Grid.Row="5">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.GroupedResources, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:ResourceTypeGroup">
                        <StackPanel Margin="0,0,0,16" Spacing="6">
                            <!-- Type section header -->
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,4">
                                <TextBlock
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind DisplayName}" />
                                <Border
                                    Padding="6,2"
                                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                    CornerRadius="8">
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind Count}" />
                                </Border>
                            </StackPanel>

                            <!-- Resources in this type -->
                            <ItemsRepeater ItemsSource="{x:Bind Resources}">
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="vm:ResourceItem">
                                        <Border
                                            Margin="0,2"
                                            Padding="12,10"
                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            PointerPressed="Resource_PointerPressed"
                                            Tag="{x:Bind}">
                                            <Grid ColumnSpacing="10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Image Width="24" Height="24" VerticalAlignment="Center">
                                                    <Image.Source>
                                                        <SvgImageSource UriSource="{x:Bind IconPath}" />
                                                    </Image.Source>
                                                </Image>
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" />
                                                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Location}" />
                                                </StackPanel>
                                                <FontIcon
                                                    Grid.Column="2"
                                                    FontSize="12"
                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                    Glyph="&#xE76C;"
                                                    VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
