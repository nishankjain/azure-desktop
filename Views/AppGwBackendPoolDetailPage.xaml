<?xml version="1.0" encoding="utf-8" ?>
<views:AppGwPageBase
    x:Class="AzureDesktop.Views.AppGwBackendPoolDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:AzureDesktop.Views"
    xmlns:controls="using:AzureDesktop.Controls"
    mc:Ignorable="d" NavigationCacheMode="Disabled">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title + Save -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Image Width="28" Height="28" VerticalAlignment="Center">
                    <Image.Source>
                        <SvgImageSource UriSource="ms-appx:///Assets/Icons/backendPools.svg" />
                    </Image.Source>
                </Image>
                <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
            <Button Grid.Column="1" Click="Save_Click" Content="Save Changes" Style="{ThemeResource AccentButtonStyle}" />
        </Grid>

        <!-- Two-column layout: Targets (left) | Rules (right) -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="32" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Backend Targets -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Targets header with search + add/delete buttons -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Text="Backend Targets" />
                    <TextBlock x:Name="TargetCountText" Grid.Column="1" FontSize="12" VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="8,0,0,0" />
                    <TextBox x:Name="TargetSearchBox" Grid.Column="3" Width="250" Margin="0,0,8,0"
                             PlaceholderText="Filter targets..." TextChanged="TargetSearch_TextChanged" />
                    <Button x:Name="AddTargetButton" Grid.Column="4" Click="AddTarget_Click"
                            Width="32" Height="32" Padding="0">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                    </Button>
                    <controls:DeleteButton x:Name="DeleteTargetsButton" Grid.Column="5" Click="DeleteTargets_Click"
                                           IsEnabled="False" Margin="4,0,0,0" />
                </Grid>

                <!-- Column headers with select-all -->
                <Grid Grid.Row="1" Margin="0,8,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="SelectAllTargets" MinWidth="0" Padding="0" Margin="0,0,8,0"
                              VerticalAlignment="Center" Checked="SelectAllTargets_Changed" Unchecked="SelectAllTargets_Changed" />
                    <TextBlock Grid.Column="1" Text="Type" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="2" Text="Value" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="8,0,0,0" VerticalAlignment="Center" />
                </Grid>

                <!-- Virtualized target list -->
                <ListView x:Name="TargetsList" Grid.Row="2" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,3" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="views:BackendTargetRow">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}" MinWidth="0" Padding="0"
                                          Margin="0,0,8,0" VerticalAlignment="Center"
                                          Checked="TargetCheckBox_Changed" Unchecked="TargetCheckBox_Changed" />
                                <ComboBox Grid.Column="1" SelectedItem="{x:Bind Type, Mode=TwoWay}" Width="110"
                                          SelectionChanged="TypeCombo_SelectionChanged" Tag="{x:Bind}">
                                    <x:String>IP</x:String>
                                    <x:String>FQDN</x:String>
                                    <x:String>VM</x:String>
                                    <x:String>VMSS</x:String>
                                    <x:String>App Service</x:String>
                                </ComboBox>
                                <!-- TextBox for IP/FQDN -->
                                <TextBox x:Name="ValueTextBox" Grid.Column="2" Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         MinWidth="200" Margin="8,0,0,0" />
                                <!-- ComboBox for resource types (VM/VMSS/App Service) -->
                                <ComboBox x:Name="ResourceCombo" Grid.Column="2" Margin="8,0,0,0" HorizontalAlignment="Stretch"
                                          ItemsSource="{x:Bind AvailableResources, Mode=OneWay}"
                                          SelectedItem="{x:Bind SelectedResource, Mode=TwoWay}"
                                          DisplayMemberPath="DisplayName"
                                          PlaceholderText="Select a resource"
                                          Visibility="Collapsed" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Right: Associated Routing Rules -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" Text="Associated Routing Rules" />
                <ListView x:Name="RulesList" Grid.Row="1" SelectionMode="None"
                          ItemContainerStyle="{StaticResource CardListViewItem}"
                          ItemClick="RuleItem_Click" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Image Width="16" Height="16" VerticalAlignment="Center">
                                    <Image.Source>
                                        <SvgImageSource UriSource="ms-appx:///Assets/Icons/routingRules.svg" />
                                    </Image.Source>
                                </Image>
                                <TextBlock Grid.Column="1" Text="{Binding}" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <FontIcon Grid.Column="2" Glyph="&#xE76C;" FontSize="12"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</views:AppGwPageBase>
