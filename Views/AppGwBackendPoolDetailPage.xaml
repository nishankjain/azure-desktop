<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AzureDesktop.Views.AppGwBackendPoolDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16,8,40,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <BreadcrumbBar x:Name="Breadcrumb" Grid.Row="0" FontSize="14" ItemClicked="Breadcrumb_ItemClicked" />

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <TextBlock x:Name="TitleText" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Left" />
            <StackPanel x:Name="StatusPanel" Orientation="Horizontal" Spacing="8" Padding="12,6" HorizontalAlignment="Right" VerticalAlignment="Center" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" Visibility="Collapsed">
                <ProgressRing x:Name="StatusSpinner" Width="14" Height="14" IsActive="True" />
                <FontIcon x:Name="StatusIcon" FontSize="14" Visibility="Collapsed" />
                <TextBlock x:Name="StatusText" FontSize="12" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,8,0,0" Orientation="Horizontal" Spacing="8">
            <Button Click="Save_Click" Content="Save Changes" Style="{ThemeResource AccentButtonStyle}" />
            <Button Click="AddTarget_Click" Content="Add Target" />
        </StackPanel>

        <!-- Targets Header + Search -->
        <StackPanel Grid.Row="3" Margin="0,12,0,0" Orientation="Horizontal" Spacing="12">
            <TextBlock FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Text="Backend Targets" />
            <TextBlock x:Name="TargetCountText" FontSize="12" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBox x:Name="TargetSearchBox" Width="250" PlaceholderText="Filter targets..." TextChanged="TargetSearch_TextChanged" />
        </StackPanel>

        <!-- Target column headers -->
        <Grid Grid.Row="4" Margin="0,8,0,4" Padding="8,4,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Type" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock Grid.Column="1" Text="Value" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="8,0,0,0" />
            <TextBlock Grid.Column="2" Text="Actions" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" />
        </Grid>

        <!-- Virtualized target list -->
        <ListView x:Name="TargetsList" Grid.Row="5" SelectionMode="None" />

        <!-- Associated Rules Section -->
        <TextBlock Grid.Row="6" FontSize="16" FontWeight="SemiBold" Margin="0,12,0,4" Text="Associated Routing Rules" />
        <StackPanel x:Name="RulesArea" Grid.Row="7" Spacing="4" Margin="0,0,0,16" />
    </Grid>
</Page>
