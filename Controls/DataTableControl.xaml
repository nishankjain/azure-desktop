<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="AzureDesktop.Controls.DataTableControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AzureDesktop.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search + Add/Delete toolbar -->
        <Grid x:Name="ToolbarGrid" Grid.Row="0" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Width="300" PlaceholderText="Search by name..." TextChanged="SearchBox_TextChanged" />
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                <Button x:Name="AddButton" Click="AddButton_Click" Style="{ThemeResource AccentButtonStyle}"
                        ToolTipService.ToolTip="Add" Visibility="Collapsed">
                    <FontIcon FontSize="14" Glyph="&#xE710;" />
                </Button>
                <controls:DeleteButton x:Name="DeleteBtn" Click="DeleteButton_Click" IsEnabled="False" ToolTipService.ToolTip="Delete selected" />
            </StackPanel>
        </Grid>

        <!-- Column headers -->
        <Grid x:Name="HeaderGrid" Grid.Row="1" Margin="0,8,0,4" />

        <!-- Data rows -->
        <ListView x:Name="DataList" Grid.Row="2" SelectionMode="None" IsItemClickEnabled="False">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,3" />
                    <Setter Property="MinHeight" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyText" Grid.Row="2" Visibility="Collapsed"
                   FontStyle="Italic" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Margin="0,20,0,0" />
    </Grid>
</UserControl>
